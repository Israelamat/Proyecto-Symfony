<nav id="navigation">
	<div class="container">
		<div id="responsive-nav">
			<ul class="main-nav nav navbar-nav">
				<li class="active">
					<a href="{{ path('home') }}">Home</a>
				</li>

				{% if is_granted('IS_AUTHENTICATED') %}
					<li>
						<a href="#">Mis juegos</a>
					</li>
					<li>
						<a href="#">Mis compras</a>
					</li>
					<li>
						<a href="#">Añadir juego</a>
					</li>
					<li>
						<a href="{{ path('user_profile') }}">Perfil</a>
					</li>
					{% if is_granted('ROLE_ADMIN') %}
					<li>
						<a href="{{ path('admin_users') }}">Gestión de usuarios</a>
					</li>
					{% endif %}
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
